#include <bits/stdc++.h>
using namespace std;


void testCase() {
    ifstream in("PTIT.in");
    ofstream out("PTIT.out");
    string s;
    while (getline(in, s)) {
        out << s << endl;
    }
    in.close();
    out.close();
}
int main () {


    testCase();
}